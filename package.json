{
  "name": "typescript-test",
  "version": "0.1.0",
  "module": "src/ui/index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "typescript-test": "./src/cli.tsx"
  },
  "scripts": {
    "prepare": "git rev-parse --git-dir > /dev/null 2>&1 && git config core.hooksPath .githooks || true",
    "ui": "bun src/cli.tsx",
    "ui:dev": "bun --watch src/cli.tsx",
    "build": "bun build src/cli.tsx --outdir dist --target bun --minify --sourcemap --packages external",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "ci": "biome ci . && tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@types/bun": "latest",
    "@types/react": "^19.2.17",
    "ink-testing-library": "^4.0.0",
    "typescript": "^5"
  },
  "dependencies": {
    "ink": "^7.1.1",
    "react": "^19.2.8"
  }
}
